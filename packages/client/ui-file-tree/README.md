# Workspace File Tree

The web-only sidebar panel for browsing the active workspace and previewing bounded text and image files. The panel uses the authenticated `workspaceFiles` Remote namespace and does not bypass the 0.1.2 BrowserAuth/Host trust boundary.
