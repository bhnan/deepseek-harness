# AGENTS.md — 交易看板（Trading Dashboard）插件功能说明

本文档介绍 DeepSeek Harness Web 里的 **A 股交易看板插件**：它是什么、有哪些部分、每个功能模块能干什么。全文以中文为准。

## 1. 这是什么

交易看板是一个嵌入 DeepSeek Harness Web 界面的 **A 股盘后看板插件**：

- 入口：GUI 左侧边栏底部的 **「交易看板」📈 按钮**，点击展开/收起。
- 形态：以覆盖面板渲染在界面中部（压缩聊天栏），顶部标题栏显示当前 **数据日**（最近有数据的交易日；管道未跑时提示"暂无数据"）。
- 定位：**只读展示**——把盘后数据管道每天生成的行情/板块/事件/信号/复盘数据，和 lab 研究仓库的生命周期对象，组织成可点击下钻的可视化界面。
- 单例：同一时刻只存在一个看板实例；与教师日历/档案等面板互斥（打开一个自动关闭另一个）。
- 热更新：看板源码变更后，页面会自动重新加载并恢复打开状态，无需手动刷新。

## 2. 功能模块（8 个页签）

看板顶部有 8 个页签：**大盘概览 / 板块 / 个股 / 组合 / 信号 / 新闻 / 实验室 / 定时**。

### 2.1 大盘概览（overview）
- **指数卡片**：上证、深成、创业板指、沪深300、中证500 五个指数卡（最新价、涨跌幅、成交额）。
- **市场宽度**：上涨家数、下跌家数、涨停/跌停家数、两市成交额（万亿）。
- **Agent 盘后复盘**：展示当天的复盘文字（summary、量能解读、涨跌结构解读），以及 **regime**（risk_on/neutral/defensive）、**trend**（up/range_bound/down）、**risk_level**（low/…/high）三枚状态徽标；若文字还是"待 Agent 生成"占位，显示"待回填（数字已注入）"提示；"今日要点"可点击弹出新闻详情。
- **申万一级行业涨跌榜**：领涨 Top6 / 领跌 Top6 横向条形榜，点击某行业直接跳转「板块」页并定位到该行业。

### 2.2 板块（sector）
- **左列：申万一级行业全列表**（按当日涨跌幅排序），列含：行业、涨跌幅、PE(TTM)、成分数、成交额(亿)；点击选中并自动滚动居中。
- **右列（选中行业联动展示）**：
  - **板块复盘**：持续性 / 扩散度 / 分歧 / 市场风格、强势板块、弱势板块、退潮信号（如领跌板块大跌给出提示）。
  - **行业详情**：历史 **K 线**（日 / 周 / 月粒度可切换），**成分股领涨 / 领跌 Top5**（当日行情），板块内 **涨跌家数统计**（涨X/跌X/平X/无行情X/共X 只）；点击成分股跳转「个股」页。
- 支持从其他页（概览行业榜、信号配对候选）跳转进入并自动定位到目标行业。

### 2.3 个股（entity）
- **搜索**：按 6 位代码或名称关键字查询（如 `600519` / `贵州`），下拉结果列表点击选中。
- **K 线**：个股历史日线（日/周/月粒度切换）。
- **简单分析**：最新收盘、近 5/20/60 日涨跌幅、MA5/MA20/MA60、均线形态（多头排列 / 空头排列 / 均线粘合）、近 60 日价格区间。
- **相关公告/新闻**：近 7 日自选范围内该股公告 + 全局快讯中提及该股的条目，时间线展示，可点击看详情。

### 2.4 组合（folio）
- **自选组合**：读取 `watchlist.json`（支持多个分组，每组含名称/代码/备注），每组展示**等权净值聚合 K 线** + 成分股列表（点击跳个股）。
- **模拟盘组合**：组合**净值曲线 vs 基准**（折线图对比），持仓表（名称/代码/占比/盈亏/状态，`near_stop` 状态有警示徽标）。

### 2.5 信号（signals）
- **策略信号卡**：顶部显示扫描状态（已扫描 / 当日未扫描 / 无启用策略）+ 参与扫描策略数；每个启用策略一张卡（策略名、当日涨跌、信号备注），点击展开详情。
- **策略详情**：
  - DSL 执行状态徽标：`DSL 已执行` / `DSL 执行失败` / `Legacy（无 DSL 表达式）`。
  - **DSL 规则**（人类可读）：旧强端（疲劳）、新弱端（修复）、配对说明。
  - **配对候选板块**：点击跳转「板块」页查看对应行业；并提供**配对候选等权净值聚合 K 线**。
  - **前向跟踪候选股票**：点击跳「个股」页，带当日涨跌幅。
  - 关联的实验/结论计数（详情见「实验室」页）。

### 2.6 新闻（news）
- **公告（自选范围）+ 快讯** 的合并时间线，含覆盖范围说明（coverage_note）。
- 点击条目弹出**详情弹窗**：正文、来源、时间、原文链接（公告来自 cninfo，快讯另有来源）。

### 2.7 实验室（lab）
以 **Idea → Signal → Experiment → Conclusion（→ Strategy）** 生命周期链路组织研究仓库内容：
- **假设池**：所有 Idea 列表（ID / 直觉命题 / 状态徽标：draft / formalized / testing / validated / rejected / archived），点击查看详情。
- **假设详情**：标题、状态、裁决（validated / rejected 等 + 裁决依据）、来源、描述、验证摘要（validation_summary）、标签。
- **关联信号**：该 Idea 形式化后的 Signal（版本号、statement 命题、DSL expression）。
- **实验与结论**：经该 Idea 关联的 Experiment 列表（purpose / status）与 Conclusion（verdict / 摘要）；点击结论弹出**完整详情弹窗**（支撑证据 / 局限 / 适用条件 / 失败条件 / 下一步实验 / 回测路径 / 置信度 / 时间）。
- **策略版本**：与该 Idea 关联的策略及其版本号。

### 2.8 定时（cron）
- **全局定时任务表**（dsh 进程内调度，交易日感知）：任务名、计划（如"每交易日 16:45 (Asia/Shanghai)"）、最近状态（运行中 / 成功 / 失败 + 最近运行时间）、操作按钮：**立即运行**、**启用/停用**。

## 3. 交互与体验约定

- **跨页下钻**：概览→板块、板块→个股、信号→板块/个股、组合/信号→个股 均可跳转；详情页有「← 返回」回到来源页；导航栈保留跳转来源。
- **详情自动滚动**：点击列表行后详情区自动滚动进视口居中，避免"点不进去"。
- **异常降级**：数据经 schema 校验，核心字段异常显示"数据异常(schema)"、缺失显示"加载中/未生成"空态，绝不带病渲染；旧日期数据自动回退到最近有数据的交易日。
- **只读**：看板不写入任何数据，全部来自 `/api/trading/*` 只读接口。

## 4. 数据来源（供理解各模块含义）

| 模块 | 数据来源 |
|---|---|
| 大盘/宽度/指数 | `data/market/`（index_spot、breadth、a_spot） |
| 板块/行业/成分股 | `data/sector/`（sw_l1_spot、sw_daily、members_spot、components） |
| 复盘 | `data/review/`（数字骨架 + Agent 生成的文字） |
| 新闻/公告 | `data/events/`（announcements） |
| 信号 | `data/signals/`（DSL 策略扫描结果）+ `lab/` 策略注册表 |
| 组合/自选 | `data/portfolio/`、`data/watchlist.json` |
| 实验室 | `lab/`（ideas / signals / strategies / experiments / conclusions / evaluations / tracking） |
| 定时任务 | `data/cron/tasks.json` + 交易日历 |

所有数据由每日盘后管道生成；个股/行业/指数/聚合日线在看板请求时按需导出。
